{{-- Kept as a thin alias: ten views include this path. The markup now lives
     in the global <x-flash> component, which the other eighteen views that
     hand-rolled their own flash markup can use directly. --}}
<x-flash />
